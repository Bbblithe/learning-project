<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE configuration
    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <!--设置别名-->
    <typeAliases>
        <!--实体类所在的包名（不是实体类的包名也可以）-->
        <package name="com.blithe.domain" />
    </typeAliases>

    <!--sql mapper(sql映射文件的)的位置-->
    <mappers>
        <!--name是包名,这个包中所有的mapper.xml文件一次都能加载
            使用package的要求：
                1.mapper文件名称和dao接口名必须完全一样，区分大小写
                2.mapper文件和dao接口必须在同一目录
        -->
        <package name="com.blithe.dao" />
    </mappers>
</configuration>