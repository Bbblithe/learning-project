<?xml version="1.0" encoding="UTF-8" ?><!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blithe.dao.StudentDao">

    <!--定义sql片段-->
    <sql id="studentSql">
        select id,name,age,email from student
    </sql>

    <!-- if

        <if test="使用参数java对象的属性值作为判断条件，语法 属性名==XXX值">

        </if>

    -->
    <select id="selectStudentIf" resultType="com.blithe.domain.Student" >
        <include refid="studentSql" />
        <if test="name!= null and name != '' ">
            name = #{name}
        </if>
        <if test="age > 0 ">
            or age > #{age}
        </if>
    </select>


    <!--
    where: <where> <if> </if> ...</where>
    -->
    <select id="selectStudentWhere" resultType="com.blithe.domain.Student">
        <include refid="studentSql" />
        <where>
            <if test="name!= null and name!= ''">
                and name = #{name}
            </if>
            <if test="age > 0">
                and age > #{age}
            </if>
        </where>
    </select>

    <select id="selectForeachOne" resultType="com.blithe.domain.Student">
        <include refid="studentSql" /> where id in
        <foreach collection="list" item="myid" open="(" close=")" separator=",">
            #{myid}
        </foreach>
    </select>
</mapper>
