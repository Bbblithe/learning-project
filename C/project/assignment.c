#include <stdio.h>

// 赋值运算符
int main(){
    // int a = 10;
    // a = 20; // = 赋值   == 判断是否相等
    // a = a+10; // 1
    // a+= 10; // 2
    // 1 2 完全等价
    return 0;

    // 复合赋值符
    // += -= *= /= %= >>= <<= &=


    // 双目操作符

    // a+b; // + 双目操作符


    // 单目操作符

}